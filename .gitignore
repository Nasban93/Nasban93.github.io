.DS_Store
*.pbix
*.pdf
# keep the downloadable CV even though PDFs are ignored
!Khalid_BinNasban_CV.pdf
Thumbs.db

# Vite build output — not committed; hr/assets/ holds the deployed copy
hr/dist/

# node_modules — never commit (platform-specific, huge)
node_modules/
**/node_modules/
