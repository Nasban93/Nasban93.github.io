PASTE EVERYTHING BELOW THIS LINE INTO CLAUDE CODE AS YOUR FIRST MESSAGE
─────────────────────────────────────────────────────────────────────

You are building a demo web app called **HotelPulse** — a unified hotel HR platform.
Read `BUILD_BRIEF.md` (focus on **Part A**) and `reference/streamlit_app.py` before writing code.
`reference/streamlit_app.py` is the original system and the source of truth for the scoring formulas.

## Build it exactly to this shape
- **Stack:** Vite + React + TypeScript, Tailwind CSS, Recharts, react-router-dom. No backend, no database server.
- **Hosting target:** a static build deployable to **GitHub Pages at `binnasban.com/hr`** (path-based). The repo is named `hr`, so set Vite `base: "/hr/"`, React Router `basename="/hr"`, and after build copy `dist/index.html` → `dist/404.html` (SPA fallback so deep links/refreshes work). Do NOT add a `CNAME` in this repo — the apex `binnasban.com` is configured once on a separate `<username>.github.io` root repo, and project repos like `hr` appear at `binnasban.com/hr/` automatically. Include a GitHub Actions deploy workflow.
- **Demo data:** synthetic, bundled as JSON, generated by a committed script. Always populated, deterministic. Show a visible "synthetic data" banner.
- **Bilingual:** English + Arabic with correct RTL layout.
- **Demo hotel:** "Najd Crown Hotel & Residences" (Arabic: فندق وأجنحة تاج نجد), Riyadh. The product/brand is "HotelPulse".

## The single most important requirement: the scoring engine must be CONFIG-DRIVEN
KPIs, their weights, and their formulas WILL change later, so they must live in config data, not in code:
- `src/config/kpiConfig.ts` — one row per KPI per department (name, weight, scoringMethod, params, active, configVersion).
- `src/config/gradeBands.ts` — grade thresholds + the coaching threshold (75).
- `src/domain/scoringMethods.ts` — a small library of generic parameterized formulas (ratioVsTarget, deduction, weightedQuestionnaire, sumDivided, passthrough). The ONLY place math lives.
- `src/domain/scoring.ts` — a GENERIC engine that reads config, runs methods, and computes `final = Σ(score×weight)/Σ(weight present)` plus the grade. It must know nothing about specific KPIs.
Seed the default config to faithfully reproduce the original Python formulas (see BUILD_BRIEF.md §6, which lists each KPI's method + params).

## Work in this order, and SHOW ME RESULTS BEFORE BUILDING UI
1. Scaffold the Vite React-TS project; install deps.
2. Write `types.ts`, then `scoringMethods.ts`, `kpiConfig.ts`, `gradeBands.ts`, and the generic `scoring.ts`.
3. Write `scoring.test.ts` (vitest) that:
   - locks the default formulas with known fixtures (use values from the original Python), AND
   - PROVES flexibility: changing a weight in config moves the final KPI, and adding a brand-new KPI via config is picked up with zero changes to `scoring.ts`.
4. Run `npm test` and show me the output. Do not proceed until tests pass.
5. Then: seed generation (~55 employees across Front Office + Housekeeping, 6 monthly periods, some <75 for coaching, ~6 exits), org rollups, then the dashboards (Department Head, HR Manager, Executive; Employee Se